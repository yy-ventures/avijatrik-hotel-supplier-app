enum AuthenticationStatus {
  unAuthenticated,
  authenticated,
}

enum ConnectionType {
  wifi,
  mobile,
}

enum BiddingType {
  open,
  negotiate,
}

enum ProductCategory {
  tour,
  holidayHome,
  hotel,
}
